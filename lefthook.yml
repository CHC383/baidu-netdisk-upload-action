pre-commit:
  jobs:
    - name: Update lockfile
      glob: "package.json"
      run: |
        pnpm install
        pnpm dedupe --ignore-scripts
      stage_fixed: true

    - name: Biome check and fix
      glob: "src/**/*.ts"
      run: pnpm biome:check-fix {staged_files}
      stage_fixed: true

    - name: Check if dist files are up to date
      glob: "{src/**/*.ts,package.json}"
      run: pnpm check-dist
      stage_fixed: true
